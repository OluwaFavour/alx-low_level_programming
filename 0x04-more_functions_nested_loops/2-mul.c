#include "main.h"

/**
  * mul - Computes the product of two intger operands
  * @a: First operand
  * @b: Second operand
  *
  * Return: Returns the result
  */
int mul(int a, int b)
{
	return (a * b);
}
